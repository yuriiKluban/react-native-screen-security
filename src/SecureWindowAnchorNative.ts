import { type ViewProps, requireNativeComponent } from 'react-native';

export type SecureWindowAnchorNativeProps = ViewProps;

export default requireNativeComponent<SecureWindowAnchorNativeProps>('RTNSecureWindowAnchor');
